
var Enum = require('enum');

var lyricsStatusEnum = new Enum({
	'ready': 1, 
	'not-available': 2,
	'no-internet': 3,
	'not-set': 4,
	'invalid': 5
});

/*

var trackEnum = new Enum({
	'ready': 1, 
	'not-available': 2,
	'no-internet': 3,
	'not-set': 4
});

*/

module.exports = {
	lyricsStatus: lyricsStatusEnum
}

//for access, use: lyricsStatusEnum.get('ready');