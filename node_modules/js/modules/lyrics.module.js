
//Includes
var events = require('js/modules/common/base.module.js');
var Enum = require('js/modules/common/enums.js');

//Private
var privateVariable = true;

module.exports = Lyrics;

function Lyrics() {
  //******* PUBLIC PROPERTIES *********
  this.state = Enum.lyricsStatus.get('not-set'); //lyrics status enum value
  this.last_download_trial_datetime = null;
}


//******* PUBLIC FUNCTIONS *********

/*
check what's up with the lyrics of this track
@Return: lyricsStatusEnum value
*/
Lyrics.prototype.getStatus = function(){
	if(this.state && this.state!='')
		return this.state;
	else
		return Enum.lyricsStatus.get('not-set');
};


Lyrics.prototype.time_since_last_trial = function(){
	if(this.last_download_trial_datetime){
		//find differnce between Now and this.last_download_trial_datetime
		//return the difference in seconds
	}

	return 0;
};